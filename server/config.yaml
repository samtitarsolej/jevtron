grid: {w: 40, h: 40}
max_ticks: 400
deadline_ms: 3000
token_budget: 20000
jev_max_tokens: 300
curveballs:
  fog_radius: null      # e.g. 5
  shrink_every: null    # e.g. 20 ticks
  bonus_tiles: 0
scoring: {performance: 0.5, efficiency: 0.3, reliability: 0.1, style: 0.1}
